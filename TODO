* Eliminate need for manually written dependency.list. This is almost exactly the
same problem I dealt with in FritoMod, so code reuse could end up with a win-win.
* Tests?! There are no tests?!
* Evaluate what's worth keeping and what we can chunk. Do we need the MochiKit
dependency, for example?
